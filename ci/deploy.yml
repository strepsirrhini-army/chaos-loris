---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8-jdk-alpine

inputs:
- name: chaos-loris

caches:
- path: m2

run:
  path: chaos-loris/ci/deploy.sh

params:
  ARTIFACTORY_USERNAME:
  ARTIFACTORY_PASSWORD:
